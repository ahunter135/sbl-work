import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.achu.worksbl',
  appName: 'work-sbl',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
